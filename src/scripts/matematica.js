export const calcPotencia = (base, expoente) => {
    return Math.pow(base, expoente);
}

export const calcRaiz = (base) => {
    return Math.sqrt(base);
}

export const calcDelta = (a, b, c) => {
    return Math.pow(b, 2) + (4 * b * c);
}